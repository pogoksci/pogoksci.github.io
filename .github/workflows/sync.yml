name: Sync to Murmurgene

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest
    if: github.repository == 'pogoksci/scimanager'
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Push to Target Repository 🪞
        run: |
          git push https://x-access-token:${{ secrets.TARGET_REPO_PAT }}@github.com/murmurgene/scimanager.git main:main --force
